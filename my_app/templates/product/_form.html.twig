{{ form_start(form, {'attr': {'class': 'product-form'}}) }}
    <div class="row gy-4">
        <div class="col-md-6">
            <div class="row align-items-center">
                <div class="col-sm-4 col-lg-5 text-sm-end">
                    {{ form_label(form.name, null, {'label_attr': {'class': 'col-form-label fw-semibold text-muted mb-0'}}) }}
                </div>
                <div class="col-sm-8 col-lg-7">
                    {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.name) }}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="row align-items-center">
                <div class="col-sm-4 col-lg-5 text-sm-end">
                    {{ form_label(form.category, null, {'label_attr': {'class': 'col-form-label fw-semibold text-muted mb-0'}}) }}
                </div>
                <div class="col-sm-8 col-lg-7">
                    {{ form_widget(form.category, {'attr': {'class': 'form-select'}}) }}
                    {{ form_errors(form.category) }}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="row align-items-center">
                <div class="col-sm-4 col-lg-5 text-sm-end">
                    {{ form_label(form.price, null, {'label_attr': {'class': 'col-form-label fw-semibold text-muted mb-0'}}) }}
                </div>
                <div class="col-sm-8 col-lg-7">
                    {{ form_widget(form.price, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.price) }}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="row align-items-center">
                <div class="col-sm-4 col-lg-5 text-sm-end">
                    {{ form_label(form.stock, null, {'label_attr': {'class': 'col-form-label fw-semibold text-muted mb-0'}}) }}
                </div>
                <div class="col-sm-8 col-lg-7">
                    {{ form_widget(form.stock, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.stock) }}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="row align-items-center">
                <div class="col-sm-4 col-lg-5 text-sm-end">
                    {{ form_label(form.image, null, {'label_attr': {'class': 'col-form-label fw-semibold text-muted mb-0'}}) }}
                </div>
                <div class="col-sm-8 col-lg-7">
                    {{ form_widget(form.image, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.image) }}
                </div>
            </div>
        </div>
        <div class="col-md-12">
            <div class="row">
                <div class="col-sm-2 col-lg-3 text-sm-end">
                    {{ form_label(form.description, null, {'label_attr': {'class': 'col-form-label fw-semibold text-muted mb-0'}}) }}
                </div>
                <div class="col-sm-10 col-lg-9">
                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': 5}}) }}
                    {{ form_errors(form.description) }}
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-end mt-4">
        <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
